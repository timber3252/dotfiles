submap = config

# hyprland
bind = , ESCAPE, submap, reset
bind = SUPER, C, submap, reset

# launch config tools
bind = SUPER, 1, exec, /home/timber3252/.scripts/pipewire-select-sinks
bind = SUPER, 1, submap, reset

# darkman toggle
bind = SUPER, 3, exec, darkmanctl toggle
bind = SUPER, 3, submap, reset

submap = reset
